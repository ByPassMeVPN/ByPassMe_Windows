<UserControl x:Class="ByPassMe.Views.OnboardingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="Transparent">
        <StackPanel VerticalAlignment="Center" Margin="32">
            <TextBlock Text="ByPassMe" FontSize="28" FontWeight="ExtraBold"
                       Foreground="{StaticResource PrimaryBrush}"
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock x:Name="ContextMessage" Text="Вставьте ссылку подписки, чтобы начать"
                       FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"
                       TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,20"/>

            <TextBlock Text="Ссылка подписки" FontSize="12"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="4,0,0,4"/>
            <TextBox x:Name="UrlInput" Style="{StaticResource ModernTextBox}"
                     AcceptsReturn="True" TextWrapping="Wrap"
                     MinHeight="60" MaxHeight="100" Margin="0,0,0,4"
                     TextChanged="OnUrlChanged"/>
            <TextBlock x:Name="ErrorText" Foreground="{StaticResource ErrorBrush}"
                       FontSize="12" Visibility="Collapsed" Margin="4,0,0,8"/>

            <Button x:Name="ConnectBtn" Content="Подключить" Style="{StaticResource PrimaryButton}"
                    Click="OnConnect" Margin="0,12,0,0" Height="54"/>
        </StackPanel>
    </Grid>
</UserControl>
